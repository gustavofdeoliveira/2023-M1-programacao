# -*- coding: utf-8 -*-
# code generated by Prisma. DO NOT EDIT.
# pyright: reportUnusedImport=false
# fmt: off
from __future__ import annotations

# global imports for type checking
from builtins import bool as _bool
from builtins import int as _int
from builtins import float as _float
from builtins import str as _str
import sys
import decimal
import datetime
from typing import (
    TYPE_CHECKING,
    Optional,
    Iterable,
    Iterator,
    Sequence,
    Callable,
    ClassVar,
    NoReturn,
    TypeVar,
    Generic,
    Mapping,
    Tuple,
    Union,
    List,
    Dict,
    Type,
    Any,
    Set,
    overload,
    cast,
)
from typing_extensions import TypedDict, Literal


LiteralString = str
# -- template actions.py.jinja --
from typing import TypeVar
import warnings

from . import types, errors, bases

if TYPE_CHECKING:
    from .client import Client
    from .bases import _PrismaModel


_PrismaModelT = TypeVar('_PrismaModelT', bound='_PrismaModel')


class CoordinateActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: 'Client', model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Coordinate]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = Coordinate.prisma().query_raw(
            'SELECT * FROM Coordinate WHERE id = ?',
            1103527590,
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Coordinate
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = Coordinate.prisma().query_first(
            'SELECT * FROM Coordinate WHERE coordinateX = ?',
            377401575.66282,
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.CoordinateCreateInput,
        include: Optional[types.CoordinateInclude] = None
    ) -> _PrismaModelT:
        """Create a new Coordinate record.

        Parameters
        ----------
        data
            Coordinate record data
        include
            Specifies which relations should be loaded on the returned Coordinate model

        Returns
        -------
        prisma.models.Coordinate
            The created Coordinate record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Coordinate record from just the required fields
        coordinate = Coordinate.prisma().create(
            data={
                # data to create a Coordinate record
                'coordinateX': 1147902781.203501,
                'coordinateY': 368800899.150802,
                'coordinateZ': 486256185.106251,
                'coordinateR': 267834847.18017,
            },
        )
        ```
        """
        resp = self._client._execute(
            operation='mutation',
            method='createOne',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return self._model.parse_obj(resp['data']['result'])

    def create_many(
        self,
        data: List[types.CoordinateCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Coordinate records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Coordinate record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = Coordinate.prisma().create_many(
            data=[
                {
                    # data to create a Coordinate record
                    'coordinateX': 836760821.59533,
                    'coordinateY': 790425851.211191,
                    'coordinateZ': 1149758321.164428,
                    'coordinateR': 1388290519.164741,
                },
                {
                    # data to create a Coordinate record
                    'coordinateX': 1675546029.176727,
                    'coordinateY': 326272115.134320,
                    'coordinateZ': 675780521.74496,
                    'coordinateR': 1969681615.111617,
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if self._client._active_provider == 'sqlite':
            raise errors.UnsupportedDatabaseError('sqlite', 'create_many()')

        resp = self._client._execute(
            operation='mutation',
            method='createMany',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.CoordinateWhereUniqueInput,
        include: Optional[types.CoordinateInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Coordinate record.

        Parameters
        ----------
        where
            Coordinate filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Coordinate model

        Returns
        -------
        prisma.models.Coordinate
            The deleted Coordinate record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        coordinate = Coordinate.prisma().delete(
            where={
                'id': 861472101,
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                operation='mutation',
                method='deleteOne',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return self._model.parse_obj(resp['data']['result'])

    def find_unique(
        self,
        where: types.CoordinateWhereUniqueInput,
        include: Optional[types.CoordinateInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Coordinate record.

        Parameters
        ----------
        where
            Coordinate filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Coordinate model

        Returns
        -------
        prisma.models.Coordinate
            The found Coordinate record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        coordinate = Coordinate.prisma().find_unique(
            where={
                'id': 1303003706,
            },
        )
        ```
        """
        resp = self._client._execute(
            operation='query',
            method='findUnique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return self._model.parse_obj(result)

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.CoordinateWhereInput] = None,
        cursor: Optional[types.CoordinateWhereUniqueInput] = None,
        include: Optional[types.CoordinateInclude] = None,
        order: Optional[Union[types.CoordinateOrderByInput, List[types.CoordinateOrderByInput]]] = None,
        distinct: Optional[List[types.CoordinateScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Coordinate records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Coordinate records returned
        skip
            Ignore the first N results
        where
            Coordinate filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Coordinate model
        order
            Order the returned Coordinate records by any field
        distinct
            Filter Coordinate records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Coordinate]
            The list of all Coordinate records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Coordinate records
        coordinates = Coordinate.prisma().find_many(take=10)

        # find the first 5 Coordinate records ordered by the coordinateY field
        coordinates = Coordinate.prisma().find_many(
            take=5,
            order={
                'coordinateY': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            operation='query',
            method='findMany',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [self._model.parse_obj(r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.CoordinateWhereInput] = None,
        cursor: Optional[types.CoordinateWhereUniqueInput] = None,
        include: Optional[types.CoordinateInclude] = None,
        order: Optional[Union[types.CoordinateOrderByInput, List[types.CoordinateOrderByInput]]] = None,
        distinct: Optional[List[types.CoordinateScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Coordinate record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Coordinate filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Coordinate model
        order
            Order the returned Coordinate records by any field
        distinct
            Filter Coordinate records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Coordinate
            The first Coordinate record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Coordinate record ordered by the coordinateZ field
        coordinate = Coordinate.prisma().find_first(
            skip=1,
            order={
                'coordinateZ': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            operation='query',
            method='findFirst',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return self._model.parse_obj(result)

    def update(
        self,
        data: types.CoordinateUpdateInput,
        where: types.CoordinateWhereUniqueInput,
        include: Optional[types.CoordinateInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Coordinate record.

        Parameters
        ----------
        data
            Coordinate record data specifying what to update
        where
            Coordinate filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Coordinate model

        Returns
        -------
        prisma.models.Coordinate
            The updated Coordinate record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        coordinate = Coordinate.prisma().update(
            where={
                'id': 1686638315,
            },
            data={
                # data to update the Coordinate record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                operation='mutation',
                method='updateOne',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return self._model.parse_obj(resp['data']['result'])

    def upsert(
        self,
        where: types.CoordinateWhereUniqueInput,
        data: types.CoordinateUpsertInput,
        include: Optional[types.CoordinateInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Coordinate filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Coordinate model

        Returns
        -------
        prisma.models.Coordinate
            The created or updated Coordinate record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        coordinate = Coordinate.prisma().upsert(
            where={
                'id': 2000430152,
            },
            data={
                'create': {
                    'id': 2000430152,
                    'coordinateX': 1675546029.176727,
                    'coordinateY': 326272115.134320,
                    'coordinateZ': 675780521.74496,
                    'coordinateR': 1969681615.111617,
                },
                'update': {
                    'coordinateX': 1675546029.176727,
                    'coordinateY': 326272115.134320,
                    'coordinateZ': 675780521.74496,
                    'coordinateR': 1969681615.111617,
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            operation='mutation',
            method='upsertOne',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return self._model.parse_obj(resp['data']['result'])

    def update_many(
        self,
        data: types.CoordinateUpdateManyMutationInput,
        where: types.CoordinateWhereInput,
    ) -> int:
        """Update multiple Coordinate records

        Parameters
        ----------
        data
            Coordinate data to update the selected Coordinate records to
        where
            Filter to select the Coordinate records to update

        Returns
        -------
        int
            The total number of Coordinate records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Coordinate records
        total = Coordinate.prisma().update_many(
            data={
                'coordinateR': 1868141281.186084
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            operation='mutation',
            method='updateMany',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.CoordinateWhereInput] = None,
        cursor: Optional[types.CoordinateWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Coordinate records present in the database

        Parameters
        ----------
        select
            Select the Coordinate fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Coordinate filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.CoordinateCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Coordinate.prisma().count()

        # results: prisma.types.CoordinateCountAggregateOutput
        results = Coordinate.prisma().count(
            select={
                '_all': True,
                'createdAt': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.CoordinateCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.CoordinateWhereInput] = None,
        cursor: Optional[types.CoordinateWhereUniqueInput] = None,
    ) -> types.CoordinateCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.CoordinateCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.CoordinateWhereInput] = None,
        cursor: Optional[types.CoordinateWhereUniqueInput] = None,
    ) -> Union[int, types.CoordinateCountAggregateOutput]:
        """Count the number of Coordinate records present in the database

        Parameters
        ----------
        select
            Select the Coordinate fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Coordinate filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.CoordinateCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Coordinate.prisma().count()

        # results: prisma.types.CoordinateCountAggregateOutput
        results = Coordinate.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            operation='query',
            method='aggregate',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.CoordinateCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.CoordinateWhereInput] = None
    ) -> int:
        """Delete multiple Coordinate records.

        Parameters
        ----------
        where
            Optional Coordinate filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Coordinate records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Coordinate records
        total = Coordinate.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            operation='mutation',
            method='deleteMany',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.CoordinateScalarFieldKeys'],
        *,
        where: Optional['types.CoordinateWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.CoordinateAvgAggregateInput'] = None,
        sum: Optional['types.CoordinateSumAggregateInput'] = None,
        min: Optional['types.CoordinateMinAggregateInput'] = None,
        max: Optional['types.CoordinateMaxAggregateInput'] = None,
        having: Optional['types.CoordinateScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.CoordinateCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.CoordinateScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.CoordinateScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.CoordinateGroupByOutput']:
        """Group Coordinate records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Coordinate fields to group records by
        where
            Coordinate filter to select records
        take
            Limit the maximum number of Coordinate records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.CoordinateGroupByOutput]
            A list of dictionaries representing the Coordinate record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Coordinate records by coordinateX values
        # and count how many records are in each group
        results = Coordinate.prisma().group_by(
            ['coordinateX'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            operation='query',
            method='groupBy',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]



def _select_fields(root: str, select: Mapping[str, Any]) -> str:
    """Helper to build a GraphQL selection string

    This is a work around until field selection is added to the query builder.
    """

    return root + ' {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))


from . import models
